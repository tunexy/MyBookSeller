class Notifier
  def update(book, more)
    puts "The use has bought #{more} books, totaling #{book.price} new Price."
    puts "The book needs to be taken in for a price cut!" 
    if car.price & amp;lt;= car.quantity
  end
end